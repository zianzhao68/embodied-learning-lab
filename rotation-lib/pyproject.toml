[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "embodied-spatial"
version = "0.1.0"
description = "Tested NumPy and PyTorch rotation utilities for embodied intelligence study"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["numpy>=1.26"]

[project.optional-dependencies]
torch = ["torch>=2.2"]
dev = ["torch>=2.2"]

[tool.setuptools.packages.find]
where = ["src"]
